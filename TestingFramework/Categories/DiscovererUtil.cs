using System;
using System.Collections.Generic;
using System.Text;

namespace TestingFramework.Categories
{
    public class DiscovererUtil
    {
        internal const string AssemblyName = nameof(TestingFramework) + "." + nameof(Categories);

    }
}
